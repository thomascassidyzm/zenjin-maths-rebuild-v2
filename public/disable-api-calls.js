/**
 * API calls interception script - DISABLED
 *
 * This script was previously intercepting API calls to /api/user-state,
 * but has been modified to allow Zustand store server persistence to work.
 */

(function() {
  console.log('[PATCH] API call interception has been DISABLED to allow server persistence');

  // No interception - allowing all API calls to proceed normally
})();